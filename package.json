{
  "name": "wait-async",
  "version": "1.0.0",
  "description": "Simple library which builds on top of wait-promise, but provides utilities for conditions being async functions.",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/adek05/wait-async.git"
  },
  "author": "Adrian Zgorzalek",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/adek05/wait-async/issues"
  },
  "homepage": "https://github.com/adek05/wait-async#readme",
  "scripts": {
    "build": "node_modules/babel-cli/bin/babel.js src --out-dir lib",
    "test": "node_modules/babel-cli/bin/babel.js test --out-dir lib_test && node --require babel-polyfill lib_test/test.js"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "dependencies": {
    "sleep-promise": "^2.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-core": "^6.17.0",
    "babel-polyfill": "^6.16.0",
    "babel-preset-es2015": "^6.16.0",
    "babel-preset-stage-0": "^6.16.0"
  }
}
